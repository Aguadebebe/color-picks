import "../../../CSS/DefineColorsArray.css";

const DefineColorsArray = ({ bra1 }) => {
    return (
        <div className="define-colors-array">const colors = {bra1}</div>
    );
}

export default DefineColorsArray;